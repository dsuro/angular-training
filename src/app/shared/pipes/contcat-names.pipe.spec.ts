import { ContcatNamesPipe } from './contcat-names.pipe';

describe('ContcatNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new ContcatNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
